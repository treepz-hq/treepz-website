import ExecutiveComponent from "@/components/executive"
import { Metadata } from 'next'

const page = () => {
    return (
        <ExecutiveComponent />
    )
}

export default page

export const metadata: Metadata = {
  title: "Treepz: Travel in Style, Arrive on Time",
};
